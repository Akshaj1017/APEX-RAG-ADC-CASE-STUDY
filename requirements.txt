# --- Web framework ---
fastapi==0.137.2
uvicorn[standard]==0.49.0
python-multipart==0.0.32        # required for file uploads (multipart/form-data)

# --- Data & storage ---
sqlmodel==0.0.38                # relational: conversations, messages, document registry
chromadb==1.5.9                 # vector store (chunks + embeddings + metadata)

# --- Models & RAG ---
mistralai>=1.0,<2.0             # embeddings + chat (v1 client API)
pypdf==5.9.0                    # PDF text extraction
rank-bm25==0.2.2                # keyword half of hybrid search

# --- Config & utils ---
pydantic==2.13.4
pydantic-settings==2.14.1       # typed settings from env / .env
PyYAML==6.0.3                   # eval golden set
httpx==0.28.1                   # used by the eval's TestClient
